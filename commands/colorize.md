---
description: Impeccable Pass 2 — contrast ratios, brand color consistency
---

Run the **/colorize** Impeccable pass on the current project, following the
"2. /colorize — CONTRAST & BRAND COLOR" checklist in `~/.claude/CLAUDE.md`.

Grep for hardcoded colors and move them to CSS custom properties. Verify contrast
ratios on every background (light, dark, image overlays, buttons, and all
hover/focus/active/disabled states). Report each color pairing you checked with its
ratio, what you changed, and anything you're flagging for Jon.
Do not mark this pass complete until the whole checklist is clean.
