---
description: Impeccable Pass 4 — entrance animations, scroll triggers, micro-interactions
---

Run the **/animate** Impeccable pass on the current project, following the
"4. /animate — MOTION & MICRO-INTERACTIONS" checklist in `~/.claude/CLAUDE.md`.

Verify entrance animations, scroll-triggered reveals, and micro-interactions are wired
and smooth. Confirm `window.LWS.observe(el)` is called for any dynamically inserted
content, transitions use consistent timing/easing via variables, and
`prefers-reduced-motion: reduce` is respected. Report what you checked, what you
changed, and anything you're flagging for Jon.
Do not mark this pass complete until the whole checklist is clean.
